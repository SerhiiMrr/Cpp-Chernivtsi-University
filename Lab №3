#include <iostream>
#include <math.h>
#include <conio.h>
#include <stdlib.h>

using namespace std;

int main()
{
    system("chcp 1251>null");
    int k=1,a,x,eps;
    do{
        cout << "ВАШ ТЕКСТ >> Введіть мм = ";
        cin >> x;
    }
    while (x == 0);
    do{
        cout << "ВАШ ТЕКСТ Введіть кол-во раз = ";
        cin >> a;
    }
    while (a == 0);

    do{
        cout << "ВАШ ТЕКСТ?.Введіть точність пошуку = ";
        cin >> eps;
    } while (eps >0);
    for(int i=0;i<x;){
        cout << "\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ"<< endl;
          cout << "ВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ\t\tВАШ ТЕКСТ"<< endl;
            }
    return 0;
}
